name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        toolchain:
          - nightly

    steps:
    - uses: actions/checkout@v4
    - uses: cachix/install-nix-action@v25
      with:
          nix_path: nixpkgs=channel:nixos-unstable
    - name: Install nix env
      run: |
        nix-shell --pure --command "which cargo"
    - name: Lint (rustfmt)
      run: nix-shell --pure --command "cargo fmt --all --check"
    - name: Lint (clippy)
      run: nix-shell --pure --command "cargo clippy --all-targets --all-features -- -D warnings"
    - name: Test (cargo)
      run: nix-shell --pure --command "cargo test"
